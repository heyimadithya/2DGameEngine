namespace ShootTheDuck;

/// <summary>
/// Global time scale for enemy motion (slow-mo bonus).
/// </summary>
public static class WorldClock
{
    public static float EnemyScale { get; set; } = 1f;
}
